{
  "name": "app1-kiro-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Kiro Agents Marketplace - Application 1",
  "scripts": {
    "prebuild": "cd ../skeleton/web && npm install",
    "build": "cd ../skeleton/web && npm run build && cd ../../app1-kiro-marketplace && npm run copy-config",
    "copy-config": "node ../scripts/copy-config.js app1",
    "dev": "cd ../skeleton/web && npm run dev",
    "preview": "cd ../skeleton/web && npm run preview"
  }
}
