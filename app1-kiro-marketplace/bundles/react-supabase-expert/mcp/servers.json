{
  "mcpServers": {
    "supabase-mcp": {
      "command": "uvx",
      "args": ["supabase-mcp-server"],
      "env": {
        "SUPABASE_URL": "${SUPABASE_URL}",
        "SUPABASE_KEY": "${SUPABASE_KEY}"
      }
    }
  }
}
